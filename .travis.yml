language: node_js
node_js:
  - '6'
  - '7'
before_install:
  - "npm install nodegit --build-from-source && npm install npm -g"
notifications:
  email: false
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - libstdc++-4.9-dev
