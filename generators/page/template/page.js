/* Page: <%= name %> */

const html = require('choo/html')

function <%= name %> (state, prev, send) {
  return html`
    <main>
      <h1><%= name %></h1>
    </main>
  `
}

module.exports = <%= name %>
